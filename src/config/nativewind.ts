// NativeWind v4 with `jsxImportSource: "nativewind"` in babel.config.js handles
// className transformations for all components at build time (including
// SafeAreaView from react-native-safe-area-context). No cssInterop registration needed.
